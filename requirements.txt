# Core
pyyaml>=6.0
numpy>=1.24.0
scipy>=1.10.0

# Audio
librosa>=0.10.0
soundfile>=0.12.0

# ML / DL
torch>=2.0.0
torchaudio>=2.0.0
pytorch-lightning==2.4.0
transformers==4.46.3

# Cloud API
openai>=1.0.0
requests>=2.28.0

# Speaker verification
# - v2 primary: speechbrain ECAPA-TDNN
# - legacy aliyun path: pyannote embedding (optional)
# Do not pin/upgrade torch here — Colab T4 already provides a compatible stack.
speechbrain>=1.0.0
pyannote.audio>=3.1.0

# Dev / test
pytest>=7.0.0

# Note: Mel-Band-Roformer and UniSE have their own requirements.
# Silero VAD is loaded via torch.hub when vad_method=auto/silero.
# On Colab T4: install with --upgrade-strategy only-if-needed so torch is not replaced.
